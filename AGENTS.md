# Project workflow

- After completing requested changes and running the appropriate checks, commit the task's changes and promptly push the current branch to its configured upstream, unless the user explicitly asks otherwise.
- Keep unrelated changes out of the commit. Do not force-push without explicit authorization.
